APP_URL=https://aimetadataeraser.vercel.app
ALLOWED_ORIGINS=https://aimetadataeraser.vercel.app
# Required on deployments. Generate with: openssl rand -base64 32
SESSION_SECRET=
# Required on deployments. No in-memory fallback outside local development/test.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Required for email login on deployments.
RESEND_API_KEY=
EMAIL_FROM=
# Optional Google sign-in. Configure the callback URL described in README.md.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# The supplied AdSense script is included by default. Set false to disable it.
ADSENSE_PUBLISHER_ID=ca-pub-8412484885269791
ADSENSE_ENABLED=true
